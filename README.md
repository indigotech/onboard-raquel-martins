# Onboard Project

# Description

Backend Project

# Environment and tools \*

-ESLint
-Prettier
-Express
-Docker
-Typescript
-Node.js

# Steps to run and debug \*

1. Run `npm i` command
2. Run `docker-compose up` command
3. Run `npm start` command
